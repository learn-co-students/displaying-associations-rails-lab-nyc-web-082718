<ul>
  <% @artists.each do |a| %>
    <li><%= link_to a.name, a %></li>
    <%= a.song_count %> songs<br>
    <% a.songs.each do |s| %>
      <%= link_to s.title, s %><br>
    <% end %>
  <% end %>
</ul>
